coverage <- read.table("coverage.merged", sep = "\t")
ggplot(coverage, aes(x=V1, y=V2)) + geom_boxplot(outlier.colour="black", outlier.shape=16, outlier.size=2, notch=FALSE, fill='#E69F00', color="black") + xlab("Samples") + ylab("Coverage") + theme_bw() + theme(panel.border = element_blank(), panel.grid.major = element_blank(), panel.grid.minor = element_blank(), panel.background = element_blank(), axis.title.x=element_text(colour = "black"), axis.text.y = element_text(colour = "black"),axis.text.x = element_text(face="bold", color="#020202", size=6, angle=45, hjust = 1, vjust = 1),  axis.ticks.x=element_blank(), axis.line.y=element_line(colour = "black")) + geom_jitter(shape=16, position=position_jitter(0.3), size=2)